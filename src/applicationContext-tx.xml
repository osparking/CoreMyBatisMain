<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-3.0.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
http://www.springframework.org/schema/aop
http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">

	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName">
			<value>org.mariadb.jdbc.Driver</value>
		</property>
		<property name="url">
			<value>jdbc:mariadb://localhost/test?useSSL=false</value>
		</property>
		<property name="username">
			<value>myself</value>
		</property>
		<property name="password">
			<value>1234</value>
		</property>
	</bean>

	<!-- Transaction manager for the data source -->
	<bean id="txManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<bean id="sqlSessionFactory"
		class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="sqlMapConfigTx.xml" />
	</bean>
	<bean id="sqlSessionTemplate"
		class="org.mybatis.spring.SqlSessionTemplate">
		<constructor-arg index="0" ref="sqlSessionFactory" />
	</bean>
	<bean id="petDAOImpl"
		class="com.learning.db.mybatis.tx.dao.PetDAOImpl">
		<property name="sqlSessionTemplate" ref="sqlSessionTemplate" />
	</bean>
	<aop:config>
		<!-- PetDAO 인터페이스에 정의된 모든 메소드의 작용에 대하여, -->
		<aop:pointcut id="petDaoOperation"
			expression="execution(* com.learning.db.mybatis.tx.dao.PetDAO.*(..))" />
		<!-- 아래 "거래에 대한 조언"이 적용됨을 확인함. -->			
		<aop:advisor pointcut-ref="petDaoOperation"
			advice-ref="txAdvice" />
	</aop:config>
	<!-- 거래에 대한 조언 -->
	<tx:advice id="txAdvice" transaction-manager="txManager">
		<tx:attributes>
			<!-- get 으로 시작하는 모든 메소드는 읽기만 함. -->
			<tx:method name="get*" read-only="true" />
			<!-- 다른 메소드는 거래 기본 속성이 설정됨. -->
			<tx:method name="*" />
		</tx:attributes>
	</tx:advice>
</beans>
